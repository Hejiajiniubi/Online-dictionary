在线词典

1. 通信协议
              请求类型     请求内容
   注册          R       name  passwd
   登录          L       name  passwd
   查单词        Q        word
   历史记录      H
   退出          E


2. 模块划分

   网络并发结构搭建
   注册
      客户端 : 输入用户名密码
              发送请求
              等待结果 分情况讨论
              yes--> 注册成功
              no --> 注册失败

      服务端 : 接收请求
              判断是否可以注册
              给客户端反馈 OK  FAIL
              yes -> 将用户信息存入数据库
              no ->

   登录
   查单词

       客户端:  输入单词
               发送请求
               等待结果
               直接打印

       服务端 : 接受请求
               查询单词
               组织结果发送给客户端
               插入历史记录

   历史记录